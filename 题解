
两个思路吧，第一个就是把编程题目给好好的刷一遍，第二个就是有时间就去看ppt，然后做思维导图。
# 1_next
很简单的一道题目要注意的是`scanf()`的时候要注意用的是int n的地址（&n）

# h01_norp
没啥要注意的，不想写

# 2_bits.c
这道题不熟悉的点还蛮多的
1. 如何用getchar读取数字
2. 用`>>` `<<` ` &` （位运算）来移动并获取二进制相应位置的值
3. 取得值之后如何把对应位置的值加成10进制

自己写代码卡顿的地方
getchar 不熟悉
while()括号中的条件是可以不能使用的比如`ch = getchar() != \n`这样是读取不到的，必须要`(ch = getchar()) != \n`
然后换行符号`\n` 也不熟悉

最重要的是& 也有点不熟悉，&是相当于把两个数自动化成二进制来判断了。

比如 31 & 8 就是相当于1111 & 0100 = 0100 = 8
16 & 8 相当于 1000 & 0100 = 0000 = 0

这样子我们就可以取出我们要的位置的数了
之后我们可以用<< 和>> 辅助，让代码变得更可读
比如我们在取第七位数的时候
<<表示所有二进制的位数左移 相当于乘方,>>表示右移
是这样的
`int threeth = (a & (1 << 2))>>2;`

当 a =31时
相当于对二进制数a 来说 1111 & 0100 得到的结果 为0100
之后我们再把第三位的数移动回去就是1

# h02 cases.c
记住char的原本的数据类型是int，可以直接加减然后输出
mad有点崩溃，说实话第二题有点难但是这个太简单了

# 3_fbits.c
这道题蛮搞说实话，一开始不知道怎么写，考察的又是小数的二进制转换。
这道题记得scanf 读double的时候用lf 哈哈哈哈哈哈哈

# h3_m2p.c
感觉这题蛮简单的一下子也就写完了，有一点小冒泡的感觉
卡住的地方是printf的地方

# 4_wdmix.c
这道题很简单，四分钟秒杀。但是这里还是要注意一下，处理string的时候记得加#include <string.h> 之后用length的时侯要用strlen(string);这个的返回值是int。 别忘了＞﹏＜

# h4_wdweight.c
这道题用时间差不多是28分，但是卡在大写转小写tolower() 还有toupper()不太熟
还有这个strtok 用的还是不太熟练，老师上课提到的时候都想不起来要怎么写。
```c
// 第一次调用 strtok 时传入待处理的字符串，第二个参数是用什么分割，这里是用空格和换行符号分隔
    char *token = strtok(str, " \n");
    // 循环处理后续的标记
    while (token != NULL) {
        printf("Token: %s\n", token);

        // 之后的调用传入 NULL，表示继续使用上一次传入的字符串
        token = strtok(NULL, " \n");
    }
```



# 5_wdust.c
不知道发生了啥问题，反正就是蛮奇怪的写在外面的时候就说啥重申明。然后记得写sqrt（）是开方吧，其他没啥好注意的。能简单写的地方就尽量简单写吧

# h5_tarea.c
妈的写个东西一个病号来了，我赶快溜溜球，这个还是蛮简单的，注意下数学公式和输入输出，真别再搞成debug半天结果输入错了吧，太倒灶了

# 6_mulrat.c
EZ
# h6_addvec.c
主要的就是计算向量和x轴的角度这一步难一些
代码我留在下面了

```c
double calAngle(Vector c){
    if(c.x == 0)
        return 0;
    double t = sqrt(c.y)/c.x;
    double ra = atan(t)* (180.0 / M_PI); // 弧度制的角度
    return ra;
}
```

# 7_no2.c
EZ 10分钟解决 怎么说呢，想提速就敢写；
一步一步实现保证正确，对于数组排序的这种题目你已经很熟练了！

# h7_missing.c
这道题就是冒泡吧，蛮简单的

# 8_taxi.c
这道题简单但是繁琐，思路清晰还是写的蛮快的；开小车哈哈哈哈哈哈哈哈哈
厌学了妈的

# h8_missing
本来这道题还好的，结果题目看错了！
retract的意思是返回上一步，不是直接加减！！！！！！！！

# 9_funsum.c
这道题秒的出，但是感觉bonus拿不到，pointer funtion不会用

# h9_dash.c
如果不拿bonus的话还蛮好拿分的。算了先把题目刷完吧

# 10_jewels.c
碰见难题暴力就完事了，别忘了 abs(int) 是绝对值！！！！！




# 13_subnet.c
这道题蛮搞笑的，回应了第二个lab。怎么说呢，对于二进制数的处理我感觉确实有提高吧。对于二进制一些方法的理解也到位了一些。
如果想让某个数位移。如果只想位移一个的话那可以直接*2，就相当于往左边位移了。感觉二进制里面还是&&用的比较多一些。
emmm怎么说呢，反正就记住<< >> 和& | 是怎么操作就行了啊
怕我不记得，再把这个写一下把 a = 1110 b = 1010   a&b = 1010 a|b = 1110 a^b = 0100
偷懒小技巧__builtin_popcount(int)； 这个函数可以直接输出int的二进制1的个数！
